Prompt: create a login page using javascript with some styles
Language: JavaScript
Timestamp: 2025-03-09 00:12:49
User: adil

--- Generated Code ---

// Import required modules
            const form = document.querySelector('.login-form');
            const usernameInput = document.querySelector('#username');
            const passwordInput = document.querySelector('#password');
            const loginBtn = document.querySelector('#login-button');
            const errorMsg = document.querySelector('#error-msg');

            // Clear error message when form is submitted
            loginBtn.addEventListener('click', (event) => {
                event.preventDefault();
                errorMsg.textContent = '';

                // Validate form inputs
                if (usernameInput.value.trim() === '' || passwordInput.value.trim() === '') {
                    errorMsg.textContent = 'Please fill in all fields';
                    return;
                }

                // Perform login logic here
                // ...

                // Display success message or navigate to dashboard
                // ...
            });

            // Prevent form submission by default
            form.addEventListener('submit', (event) => {
                event.preventDefault();
            });

            // Add styles for input error
            const inputErrors = document.querySelectorAll('.error');
            inputErrors.forEach((inputError) => {
                inputError.style.display = 'none';
            });

            // Show error message for invalid input
            const showInputError = (input, message) => {
                const error = document.createElement('p');
                error.classList.add('error');
                error.textContent = message;
                input.parentNode.insertBefore(error, input.nextElementSibling);
                input.style.borderColor = 'red';
            };

            // Validate username input
            usernameInput.addEventListener('blur', () => {
                if (usernameInput.value.trim() === '') {
                    showInputError(usernameInput, 'Username cannot be empty');
                }
            });

            // Validate password input
            passwordInput.addEventListener('blur', () => {
                if (passwordInput.value.trim() === '') {
                    showInputError(passwordInput, 'Password cannot be empty');
                }
            });