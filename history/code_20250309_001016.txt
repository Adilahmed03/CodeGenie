Prompt: create a responsive javascript page

Language: JavaScript
Timestamp: 2025-03-09 00:10:16
User: adil

--- Generated Code ---

// Define variables
            const viewportWidth = window.innerWidth;
            const viewportHeight = window.innerHeight;

            // Define function to check screen size
            function checkScreenSize() {
                const screenWidth = window.innerWidth;
                const screenHeight = window.innerHeight;

                if (screenWidth <= 480) {
                    // Set body to mobile class
                    document.body.classList.add('mobile');
                    document.body.classList.remove('desktop');
                } else {
                    // Set body to desktop class
                    document.body.classList.add('desktop');
                    document.body.classList.remove('mobile');
                }
            }

            // Run function on page load
            checkScreenSize();

            // Run function on window resize
            window.addEventListener('resize', checkScreenSize);

            // Define media queries for different screen sizes
            @media only screen and (max-width: 480px) {
                /* Styles for mobile */
            }

            @media only screen and (min-width: 481px) and (max-width: 768px) {
                /* Styles for small tablets */
            }

            @media only screen and (min-width: 769px) {
                /* Styles for medium tablets and desktops */
            }

            @media only screen and (min-width: 1024px) {
                /* Styles for large desktops */
            }

            // Define variables for media queries
            const mediaQueries = {
                mobile: matchMedia('(max-width: 480px)'),
                smallTablets: matchMedia('(min-width: 481px) and (max-width: 768px)'),
                mediumTablets: matchMedia('(min-width: 769px)'),
                largeDesktops: matchMedia('(min-width: 1024px)')
            };

            // Run function on page load to